import 'package:flutter/material.dart';

class QuizModeScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Quiz Mode'),
      ),
      body: Center(
        child: Text('Quiz Mode Screen'),
      ),
    );
  }
}
