import 'package:flutter/material.dart';

class CreateFlashcardScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Create Flashcards'),
      ),
      body: Center(
        child: Text('Create Flashcard Screen'),
      ),
    );
  }
}
